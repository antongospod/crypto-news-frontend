name: Blue-Green Deploy to VPS

on:
  push:
    branches: [master]

concurrency:
  group: production-deploy
  cancel-in-progress: false

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Deploy to VPS
        uses: appleboy/ssh-action@v1.0.0
        with:
          host: ${{ secrets.HOST }}
          username: ${{ secrets.USERNAME }}
          key: ${{ secrets.PRIVATE_KEY }}
          port: ${{ secrets.PORT }}
          script: |
            cd /opt/cryptocenter
            echo "🚀 Starting Blue-Green deployment..."

            # Обновляем код
            git stash push -m "Auto-stash $(date)" || true
            git reset --hard HEAD
            git pull origin master

            # Определяем текущий активный порт из nginx конфига
            CURRENT_PORT=$(grep -o "172.20.0.1:[0-9]*" nginx.conf | head -1 | cut -d: -f2)

            if [ "$CURRENT_PORT" = "3000" ]; then
              NEW_PORT=3002
              COLOR="🟢 Green"
              echo "🔵 Current: Blue (3000) → Deploying: $COLOR ($NEW_PORT)"
            else
              NEW_PORT=3000
              COLOR="🔵 Blue"
              echo "🟢 Current: Green (3002) → Deploying: $COLOR ($NEW_PORT)"
            fi

            # Собираем новый образ
            echo "🔨 Building new version..."
            docker compose build nuxt-app

            # Убиваем staging контейнер если есть
            echo "🧹 Cleaning up staging container..."
            docker stop nuxt-app-staging 2>/dev/null || true
            docker rm -f nuxt-app-staging 2>/dev/null || true

            # Убиваем процессы на NEW_PORT (не трогаем CURRENT_PORT!)
            echo "🔫 Freeing port $NEW_PORT..."
            fuser -k $NEW_PORT/tcp 2>/dev/null || true
            sleep 2

            # Запускаем новый контейнер на NEW_PORT
            echo "🚀 Starting new version on port $NEW_PORT..."
            docker run -d \
              --name nuxt-app-staging \
              --network cryptocenter_app-network \
              -p $NEW_PORT:3000 \
              -e NODE_ENV=production \
              -e NUXT_PUBLIC_SITE_NAME=CryptoCenter \
              -e NUXT_PUBLIC_SITE_URL=https://cryptocenter.finance \
              cryptocenter-nuxt-app

            # Ждем запуска
            echo "⏳ Waiting for new version to start..."
            sleep 20

            # Health check новой версии
            echo "🔍 Health checking new version..."
            HEALTH_OK=false
            for i in {1..10}; do
              if curl -f -s http://127.0.0.1:$NEW_PORT > /dev/null; then
                echo "✅ New version is healthy"
                HEALTH_OK=true
                break
              fi
              echo "⏳ Attempt $i/10 - waiting..."
              sleep 3
            done

            if [ "$HEALTH_OK" != "true" ]; then
              echo "❌ New version failed health check!"
              docker logs nuxt-app-staging --tail 50
              docker stop nuxt-app-staging
              docker rm nuxt-app-staging
              exit 1
            fi

            # Переключаем nginx на новый порт
            echo "🔄 Switching nginx to new version..."
            cp nginx.conf nginx.conf.backup
            sed -i "s/172.20.0.1:$CURRENT_PORT/172.20.0.1:$NEW_PORT/g" nginx.conf

            # Проверяем конфигурацию nginx перед reload
            echo "🔍 Testing nginx configuration..."
            docker exec nginx nginx -t
            if [ $? -ne 0 ]; then
              echo "❌ Nginx configuration test failed!"
              cp nginx.conf.backup nginx.conf
              docker stop nuxt-app-staging
              docker rm nuxt-app-staging
              exit 1
            fi

            # Перезагружаем nginx конфигурацию
            echo "🔄 Reloading nginx..."
            docker exec nginx nginx -s reload

            # Даем время на применение изменений
            sleep 5

            # Проверяем что nginx переключился успешно
            # Используем несколько методов проверки
            echo "🔍 Checking if nginx switched successfully..."

            # Метод 1: Проверка через localhost (обход SSL)
            NGINX_OK=false
            if curl -f -s -H "Host: cryptocenter.finance" http://localhost > /dev/null 2>&1; then
              echo "✅ Nginx responds on localhost"
              NGINX_OK=true
            fi

            # Метод 2: Проверка через внутренний health endpoint
            if [ "$NGINX_OK" = "true" ] && curl -f -s http://localhost/health > /dev/null 2>&1; then
              echo "✅ Health endpoint responds"
              NGINX_OK=true
            else
              # Метод 3: Проверка через внешний домен с игнорированием SSL
              if curl -f -s -k https://cryptocenter.finance > /dev/null 2>&1; then
                echo "✅ External domain responds (SSL ignored)"
                NGINX_OK=true
              fi
            fi

            # Метод 4: Проверка что nginx процесс работает и слушает порты
            if docker exec nginx ss -tlnp | grep -q ":443"; then
              echo "✅ Nginx is listening on port 443"
            fi

            if [ "$NGINX_OK" = "true" ]; then
              echo "✅ Nginx successfully switched to new version"

              # ТОЛЬКО ТЕПЕРЬ останавливаем старую версию
              echo "🛑 Stopping old version on port $CURRENT_PORT..."

              # Если текущий порт 3000, то останавливаем основной контейнер
              if [ "$CURRENT_PORT" = "3000" ]; then
                docker compose stop nuxt-app 2>/dev/null || true
                # Убиваем процесс на старом порту если контейнер не остановился
                sleep 2
                fuser -k $CURRENT_PORT/tcp 2>/dev/null || true
              else
                # Если текущий порт 3002, убиваем процесс на нем
                fuser -k $CURRENT_PORT/tcp 2>/dev/null || true
              fi

              echo "✅ $COLOR deployment completed successfully!"

            else
              echo "❌ Nginx switch failed, rolling back..."
              cp nginx.conf.backup nginx.conf
              docker exec nginx nginx -s reload
              docker stop nuxt-app-staging
              docker rm nuxt-app-staging

              # Показываем логи для диагностики
              echo "📋 Nginx error logs:"
              docker exec nginx tail -n 20 /var/log/nginx/error.log

              exit 1
            fi

            # Обновляем остальные сервисы если нужно
            echo "🔄 Updating other services..."
            docker compose up -d --no-deps nginx prometheus grafana cadvisor node-exporter

            # Проверяем финальный статус
            echo "📊 Final status:"
            docker ps --format "table {{.Names}}\t{{.Status}}\t{{.Ports}}"

            # Показываем какие порты слушаются
            echo "🔍 Active ports:"
            ss -tlnp | grep -E ":(80|443|3000|3002)" || netstat -tlnp | grep -E ":(80|443|3000|3002)"

            # Очистка
            echo "🧹 Cleaning up..."
            docker system prune -f

            # Финальная проверка доступности
            echo "🌐 Final availability check:"
            curl -I -s -k https://cryptocenter.finance | head -n 1

            echo "✅ Blue-Green deployment completed!"
            echo "🌐 Site available at: https://cryptocenter.finance"
